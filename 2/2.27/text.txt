int i = -1, &r = 0;
非法
int *const p2 = &i2;
合法    p2 指向int的常量指针
const int i = -1, &r = 0;
合法    r 是引用int常量 0
const int *const p3 = &i2;
合法    p3 指向int常量的常量指针
const int *p1 = &i2;
合法    p1 指向int常量的指针
const int &const r2;
非法
const int i2 = i, &r = i;
合法    i2 int常量  r 引用int常量
